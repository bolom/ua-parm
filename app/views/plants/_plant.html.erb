<div class="block mb-2">

<%= plant.scientific %>,
<%= plant.family %>,
<%= plant.pharmacopoeia %>

  <div class="inline-block ml-2">
    <%= link_to "Edit", edit_plant_path(plant),
             class: "btn bg-gray-100"
 %>
  <%= button_to "Delete", plant_path(plant),
                method: :delete,
                data: { action: "plants#delete" },
                class: "btn bg-red-100" %>
</div>

</div>
