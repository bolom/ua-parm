<div>
  <h1 class="font-bold text-2xl mb-3">Editing Plant</h1>

  <div id="<%= dom_id @plant %>">
    <%= render "form", plant: @plant %>
    <%= link_to "Never Mind", plants_path, class: "btn mb-3 bg-gray-100" %>
  </div>
</div>
