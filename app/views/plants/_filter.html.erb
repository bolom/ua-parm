<%= form_with(url: plants_path,
             class: "mt-5 mb-5",
             method: :get, data: { controller: "filters", filters_target: "form", turbo_frame: "plants" }) do |form| %>

 <div class="form-group mb-6 inline-flex space-x-2">
   <%= form.select :pharmacopoeia, ["Tramil", "Ayurvédique/Indienne", "none"],
                  { include_blank: true },
                   { data: { action: "filters#submit" } }%>
  <%= form.select :family, ["Euphorbiaceae", "Euphorbiaceae2"],
                                  { include_blank: true },
                                   { data: { action: "filters#submit" } }%>
  <%= form.text_field :search,
                     {placeholder: "Search for",
                     value: params[:search], class: "form-control block w-full px-3 py-1.5 text-base font-normal text-gray-700 bg-white bg-clip-padding  border border-solid border-gray-300 rounded transition  ease-in-out m-0 focus:text-gray-700 focus:bg-white focus:border-blue-600 focus:outline-none", data: { action: "filters#submit" }} %>
</div>
<% end %>
