<div class="w-full">
  <h1 class="font-bold text-4xl">New Citation</h1>
  <%= turbo_frame_tag @citation do %>
    <%= render "form", citation: @citation, name_button: "create a new citation" %>
  <% end %>
</div>
