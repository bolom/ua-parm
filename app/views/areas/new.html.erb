<div>
  <h1 class="font-bold text-2xl mb-3">New Area</h1>

  <div id="<%= dom_id @area %>">
    <%= render "form", area: @area %>
    <%= link_to "Never Mind", areas_path, class: "btn mb-3 bg-gray-100" %>
  </div>
</div>
